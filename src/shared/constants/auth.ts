/**
 * Default dashboard password for first-run setup.
 * Used when no password has been configured yet.
 * Must be kept in sync across login route and ensureDefaultPassword.
 */
export const DEFAULT_DASHBOARD_PASSWORD = "12345677";
