const runtimeDefaults = require("../shared/constants/runtimeDefaults.json");

const DEFAULT_AXONROUTER_PORT = runtimeDefaults.defaultPort;
const DEFAULT_AXONROUTER_BASE_URL = runtimeDefaults.defaultBaseUrl;
const DEFAULT_AXONROUTER_API_BASE_URL = runtimeDefaults.defaultApiBaseUrl;

module.exports = {
  DEFAULT_AXONROUTER_PORT,
  DEFAULT_AXONROUTER_BASE_URL,
  DEFAULT_AXONROUTER_API_BASE_URL,
};
